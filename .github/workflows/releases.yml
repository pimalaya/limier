name: Releases

on:
  push:
    branches:
      - master
    tags:
      - v*

jobs:
  apk:
    runs-on: ubuntu-24.04
    env:
      LIMIER_KEYSTORE_PASSWORD: ${{ secrets.LIMIER_KEYSTORE_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: experimental-features = nix-command flakes
      - uses: cachix/cachix-action@v16
        with:
          name: soywod
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          extraPullNames: nix-community
      # Decode the keystore only when the secret is present; otherwise the
      # build proceeds and emits an unsigned APK.
      - name: Decode signing keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.LIMIER_KEYSTORE_BASE64 }}
        run: |
          if [ -n "$KEYSTORE_BASE64" ]; then
            echo "$KEYSTORE_BASE64" | base64 -d > "$RUNNER_TEMP/limier.jks"
            echo "LIMIER_KEYSTORE=$RUNNER_TEMP/limier.jks" >> "$GITHUB_ENV"
          fi
      - name: Assemble release APKs
        run: nix develop --command bash -c 'cd android && gradle :app:assembleRelease'
      # Rename each split (and the universal) to its published name; the
      # *.apk glob absorbs the -unsigned suffix when no keystore is set.
      - name: Collect the APKs
        run: |
          out=android/app/build/outputs/apk/release
          cp "$out"/app-universal-release*.apk limier.apk
          for abi in arm64-v8a armeabi-v7a x86_64 x86; do
            cp "$out/app-$abi-release"*.apk "limier-$abi.apk"
          done
      - uses: actions/upload-artifact@v4
        with:
          name: limier-apk
          path: limier.apk
      - uses: actions/upload-artifact@v4
        with:
          name: limier-arm64-v8a-apk
          path: limier-arm64-v8a.apk
      - uses: actions/upload-artifact@v4
        with:
          name: limier-armeabi-v7a-apk
          path: limier-armeabi-v7a.apk
      - uses: actions/upload-artifact@v4
        with:
          name: limier-x86_64-apk
          path: limier-x86_64.apk
      - uses: actions/upload-artifact@v4
        with:
          name: limier-x86-apk
          path: limier-x86.apk
      - uses: softprops/action-gh-release@v2
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        with:
          files: |
            limier.apk
            limier-arm64-v8a.apk
            limier-armeabi-v7a.apk
            limier-x86_64.apk
            limier-x86.apk
